<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.change.demo002.dao.UserDao">
    <insert id="findUser" parameterType="String" >
        insert into register(username, phone, password, admin, agree) values(#{username}, #{phone}, #{password}, #{admin}, #{agree})
    </insert>
    <select id="loginUser" parameterType="String" resultType="User">
        select * from register where username = #{username} and password = #{password}
    </select>
    <!--<delete id="deleUser" parameterType="String">-->
        <!--DELETE FROM register WHERE  id=#{id};-->
    <!--</delete>-->
</mapper>