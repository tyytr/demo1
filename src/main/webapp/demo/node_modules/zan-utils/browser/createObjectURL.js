(function (global, factory) {
  if (typeof define === "function" && define.amd) {
    define(["module", "exports"], factory);
  } else if (typeof exports !== "undefined") {
    factory(module, exports);
  } else {
    var mod = {
      exports: {}
    };
    factory(mod, mod.exports);
    global.createObjectURL = mod.exports;
  }
})(this, function (module, exports) {
  "use strict";

  Object.defineProperty(exports, "__esModule", {
    value: true
  });
  /**
   * Normalize URL.createObjectURL, webkitURL.createObjectURL and don't crash older browsers
   * @memberof module:browser
   * @param {blob} object 用来创建URL的File对象或者Blob对象
   * @return {object}  URL 对象
   * @example
   * import createObjectURL from 'zan-utils/browser/createObjectURL';
   * const objectURL = createObjectURL(blob);
   */
  function createObjectURL(object) {
    return window.URL ? window.URL.createObjectURL(object) : window.webkitURL.createObjectURL(object);
  }
  exports["default"] = createObjectURL;
  module.exports = exports["default"];
});